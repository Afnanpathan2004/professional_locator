specVersion: "v1beta"
serviceId: "professional_locator"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "professional_locator-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
